---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: metricproviderclasses.xas.io
spec:
  group: xas.io
  names:
    kind: MetricProviderClass
    listKind: MetricProviderClassList
    plural: metricproviderclasses
    singular: metricproviderclass
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MetricProviderClass defines a reusable configuration for a metric
          source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              config:
                additionalProperties:
                  type: string
                description: Config is a map of static configuration passed to the
                  provider.
                type: object
              type:
                description: Type defines the provider implementation (e.g. "Prometheus",
                  "Kubelet").
                type: string
            required:
            - type
            type: object
        type: object
    served: true
    storage: true
